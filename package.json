{
  "name": "@snaplife/graphql-service",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "build:lambda": "tsc && cp package.json dist/",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "package:lambda": "npm run build:lambda && cd dist && npm install --production && zip -r ../lambda-deployment.zip ."
  },
  "dependencies": {
    "@apollo/server": "^4.12.2",
    "@as-integrations/aws-lambda": "^3.1.0",
    "apollo-server": "^3.13.0",
    "apollo-server-errors": "^3.3.1",
    "axios": "^1.10.0",
    "dotenv": "^17.2.1",
    "graphql": "^16.11.0",
    "graphql-type-json": "^0.3.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
